# 项目改动记录规约

## 适用范围

本规约适用于仓库根目录及其全部子目录中的代码、配置、数据库脚本、部署文件和文档改动。

## 必须遵守

1. 每次完成一项独立改动，都必须在 `docs/changes/` 下新增一份对应的改动记录。
2. 改动记录必须与实际修改在同一次任务中完成，不得只改代码而遗漏文档。
3. 文档名必须使用中文主题，并以改动日期的月日结尾，格式为 `<中文改动主题>_MMDD.md`，例如 `改动1_0723.md`。
4. 同一天出现同名主题时不得覆盖已有记录，应追加序号，例如 `改动1_0723_2.md`。
5. 文档正文必须写明精确变更时间，格式为 `YYYY-MM-DD HH:mm:ss +08:00`。
6. 文档至少包含以下内容：变更信息、变更内容、验证结果。存在兼容性要求、运行方式或未完成事项时，应增加相应说明。
7. 变更内容必须基于真实修改，不得记录未实施或未验证的事项；无法执行的验证需要明确说明原因。
8. 一个文档对应一项完整、可描述的改动。后续独立改动应创建新文档，不得持续向旧文档混入无关内容。

## 完成前检查

- 确认本次改动对应的 `docs/changes/*.md` 已创建。
- 确认文件名为中文并带 `MMDD` 日期。
- 确认正文时间、涉及文件、行为变化和验证结果准确。
- 确认没有覆盖或删除历史改动记录。

