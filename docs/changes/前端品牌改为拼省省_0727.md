# 前端品牌改为拼省省

## 变更信息

- 变更时间：2026-07-27 19:00:18 +08:00
- 变更范围：`docs/dev-ops/nginx/html` 下商城首页、商品详情、订单中心、模拟用户中心和运营后台入口页
- 变更类型：前端展示文案调整

## 变更内容

- 将首页浏览器标题和主品牌从 `HJS 拼团` 改为 `拼省省`。
- 将商品详情页静态标题、顶部标题和动态商品标题改为 `拼省省`。
- 将模拟用户中心、订单中心、运营后台和运营后台登录页中的浏览器标题及品牌文案统一改为 `拼省省`。
- 将模拟用户注册页的用户 ID 示例从 `hjs_user_a` 改为 `pss_user_a`。
- 保留本地模拟用户的 `localStorage` 存储 key，不清空已有本地身份数据。

## 验证结果

- `rg` 检查 `docs/dev-ops/nginx/html` 下 HTML/JS 文件，未发现可见旧品牌 `HJS` 或旧示例 `hjs_user`。
- `node --check` 检查首页、登录、订单、商品详情和运营后台 JavaScript，全部通过。
- 使用本机 Chrome 验证 `index.html`、`login.html`、`orders.html`、`product-detail.html?goodsId=9890001` 和 `admin/login.html`，浏览器标题与页面主品牌均按 `拼省省` 展示。

