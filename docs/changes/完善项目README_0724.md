# 完善项目 README

## 变更信息

- 变更时间：2026-07-24 00:35:33 +08:00
- 变更类型：项目说明文档重写
- 涉及文件：`README.md`

## 变更内容

- 将根目录中原有的脚手架残留说明重写为当前拼团交易项目的完整介绍。
- 项目标题统一为 `hjs-group-buy-market-study`，示例用户统一使用 `hjs01`。
- 增加核心能力、技术栈、业务流程图、DDD 模块职责和工程目录说明。
- 增加本机 MySQL、Redis、RabbitMQ、后端和前端的端口与开发账号说明。
- 增加 Docker Compose、中间件初始化、Maven 构建、IDEA 启动和命令行启动步骤。
- 增加拼团全流程前端的启动方式、页面入口和操作范围。
- 增加试算、锁单、结算、退款和动态配置接口的请求示例。
- 增加环境变量、RabbitMQ 拓扑、验证命令和停止服务方式。
- 增加端口占用、接口限流、队列无积压、数据库初始化和前端连接问题的排查说明。
- 明确 MySQL 初始化脚本只在命名卷首次创建时自动执行，避免误删共享数据卷。

## 验证结果

- README 中未发现旧脚手架命名文本。
- README 中存在 `hjs` 项目标题、Java 包路径和接口示例用户。
- README 引用的启动类、Compose 文件、SQL 脚本和前端目录均存在。
- README 中记录的本机端口、账号和环境变量与当前 `application-dev.yml`、Compose 配置一致。
- `docker compose config` 验证通过。
- `git diff --check` 验证通过。

## 说明

- 本次只修改项目说明文档，没有变更 Java 业务逻辑、数据库结构或外部接口。
- README 中的账号密码仅适用于绑定本机回环地址的开发环境，生产环境必须通过安全配置覆盖。
