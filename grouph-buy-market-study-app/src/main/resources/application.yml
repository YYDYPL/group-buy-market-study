# 应用公共入口配置：应用名与默认环境已迁移到 bootstrap.yml（Nacos 配置中心需要在引导阶段确定）。
# 此处仅保留与业务相关的全局默认值，具体资源连接参数由 profile 文件提供。
spring:
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 6MB

gbm:
  admin:
    token: ${GBM_ADMIN_TOKEN:}
    upload-dir: ${GBM_UPLOAD_DIR:./uploads}
