<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hjs.study.infrastructure.dao.IGroupBuyOrderListDao">

    <!--
        group_buy_order_list 表结果映射。
        这张表保存的是“用户级订单明细”，记录单个用户参与某个队伍的订单事实，
        包括锁单、支付、退单、外部交易单号等信息。
    -->
    <resultMap id="dataMap" type="com.hjs.study.infrastructure.dao.po.GroupBuyOrderList">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="team_id" property="teamId"/>
        <result column="order_id" property="orderId"/>
        <result column="activity_id" property="activityId"/>
        <result column="start_time" property="startTime"/>
        <result column="end_time" property="endTime"/>
        <result column="goods_id" property="goodsId"/>
        <result column="source" property="source"/>
        <result column="channel" property="channel"/>
        <result column="original_price" property="originalPrice"/>
        <result column="deduction_price" property="deductionPrice"/>
        <result column="pay_price" property="payPrice"/>
        <result column="status" property="status"/>
        <result column="out_trade_no" property="outTradeNo"/>
        <result column="out_trade_time" property="outTradeTime"/>
        <result column="biz_id" property="bizId"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!--
        新增一条用户参团明细。
        锁单成功后会写入这里，作为营销侧的用户订单记录。
    -->
    <insert id="insert" parameterType="com.hjs.study.infrastructure.dao.po.GroupBuyOrderList">
        insert into group_buy_order_list(
        user_id, team_id, order_id, activity_id, start_time,
        end_time, goods_id, source, channel, original_price, pay_price,
        deduction_price, status, out_trade_no, biz_id, create_time, update_time
        )
        values(
        #{userId}, #{teamId}, #{orderId}, #{activityId}, #{startTime},
        #{endTime}, #{goodsId}, #{source}, #{channel}, #{originalPrice}, #{payPrice},
        #{deductionPrice}, #{status}, #{outTradeNo}, #{bizId}, now(), now()
        )
    </insert>

    <!--
        按 userId + outTradeNo 查询营销订单记录。
        这是锁单接口幂等校验的重要 SQL，用于判断外部单号是否已经被处理过。
    -->
    <select id="queryGroupBuyOrderRecordByOutTradeNo"
            parameterType="com.hjs.study.infrastructure.dao.po.GroupBuyOrderList" resultMap="dataMap">
        select user_id, team_id, order_id, activity_id, start_time,
        end_time, goods_id, source, channel, original_price, deduction_price, pay_price, status
        from group_buy_order_list
        where out_trade_no = #{outTradeNo} and user_id = #{userId}
    </select>

    <!--
        统计用户在某活动下的参团次数。
        常用于校验 take_limit_count，即用户参与次数上限。
    -->
    <select id="queryOrderCountByActivityId" parameterType="com.hjs.study.infrastructure.dao.po.GroupBuyOrderList"
            resultType="java.lang.Integer">
        select count(id) from group_buy_order_list
        where user_id = #{userId} and activity_id = #{activityId}
    </select>

    <!--
        将订单明细从“初始锁定”更新为“支付完成”，并写入外部支付时间。
        通过 status = 0 约束，避免重复结算同一笔订单。
    -->
    <update id="updateOrderStatus2COMPLETE" parameterType="com.hjs.study.infrastructure.dao.po.GroupBuyOrderList">
        update group_buy_order_list
        set status = 1, out_trade_time = #{outTradeTime}, update_time = now()
        where out_trade_no = #{outTradeNo} and user_id = #{userId} and status = 0
    </update>

    <!-- 查询某个队伍下所有已支付成功成员的外部交易单号列表。 -->
    <select id="queryGroupBuyCompleteOrderOutTradeNoListByTeamId" parameterType="java.lang.String"
            resultType="java.lang.String">
        select out_trade_no from group_buy_order_list where team_id = #{teamId} and status = 1
    </select>

    <!--
        查询指定用户在某活动下仍在进行中的拼团记录。
        条件说明：
        1. status in (0,1)，锁定中或已支付都算进行中；
        2. end_time > now()，活动未结束；
        3. 按 id 倒序后限制条数，优先取最近参与的记录。
    -->
    <select id="queryInProgressUserGroupBuyOrderDetailListByUserId"
            parameterType="com.hjs.study.infrastructure.dao.po.GroupBuyOrderList" resultMap="dataMap">
        select user_id, team_id, out_trade_no
        from group_buy_order_list
        where activity_id = #{activityId} and user_id = #{userId} and status in (0, 1) and end_time > now()
        order by id desc
        limit #{count}
    </select>

    <!--
        查询同活动下其他用户正在进行中的拼团记录。
        典型用途是“大家都在拼”的随机展示。
        这里先通过 SQL 过滤出候选队伍，真正的随机抽样会在 Java 侧继续处理。
    -->
    <select id="queryInProgressUserGroupBuyOrderDetailListByRandom"
            parameterType="com.hjs.study.infrastructure.dao.po.GroupBuyOrderList" resultMap="dataMap">
        select user_id, team_id, out_trade_no
        from group_buy_order_list
        where
            activity_id = #{activityId} and
            team_id in (select team_id from group_buy_order where activity_id = #{activityId} and status = 0) and
            user_id != #{userId} and
            status in (0, 1)
            and end_time > now()
        order by id desc
        limit #{count}
    </select>

    <!--
        查询某活动下所有进行中的队伍代表记录。
        这里通过 group by team_id 做队伍去重，供统计逻辑继续汇总队伍数据。
    -->
    <select id="queryInProgressUserGroupBuyOrderDetailListByActivityId" parameterType="java.lang.Long"
            resultMap="dataMap">
        select user_id, team_id, out_trade_no
        from group_buy_order_list
        where activity_id = #{activityId} and status in (0, 1)
        group by team_id
    </select>

    <!-- 未支付即退单：只允许从 status = 0 更新到 status = 2。 -->
    <update id="unpaid2Refund" parameterType="com.hjs.study.infrastructure.dao.po.GroupBuyOrderList">
        update group_buy_order_list
        set status = 2, update_time = now()
        where
            user_id = #{userId} and
            order_id = #{orderId} and
            status = 0
    </update>

    <!-- 已支付但未成团退单：只允许从 status = 1 更新到 status = 2。 -->
    <update id="paid2Refund" parameterType="com.hjs.study.infrastructure.dao.po.GroupBuyOrderList">
        update group_buy_order_list
        set status = 2, update_time = now()
        where
            user_id = #{userId} and
            order_id = #{orderId} and
            status = 1
    </update>

    <!--
        已成团后成员退单。
        对明细表来说仍然只是把该成员订单标记为退单，
        具体队伍变成“完成含退单”还是“失败”由主单表决定。
    -->
    <update id="paidTeam2Refund" parameterType="com.hjs.study.infrastructure.dao.po.GroupBuyOrderList">
        update group_buy_order_list
        set status = 2, update_time = now()
        where
            user_id = #{userId} and
            order_id = #{orderId} and
            status = 1
    </update>

    <!--
        扫描超时未支付订单。
        条件说明：
        1. status = 0，仍处于锁定态；
        2. out_trade_time is null，说明尚未支付；
        3. now() > end_time，说明订单有效期已结束。
        当前单次最多取 10 条，适合定时任务分批处理。
    -->
    <select id="queryTimeoutUnpaidOrderList" resultMap="dataMap">
        select user_id, team_id, order_id, activity_id, start_time,
               end_time, goods_id, source, channel, original_price, deduction_price, 
               pay_price, status, out_trade_no, out_trade_time, source, channel
        from group_buy_order_list
        where status = 0 
          and out_trade_time is null 
          and now() > end_time
        limit 10
    </select>

</mapper>
