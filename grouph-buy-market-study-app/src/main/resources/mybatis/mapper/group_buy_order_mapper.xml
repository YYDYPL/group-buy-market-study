<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hjs.study.infrastructure.dao.IGroupBuyOrderDao">

    <!--
        group_buy_order 表结果映射。
        这张表表示“队伍主单”，关注的是拼团队伍整体状态，
        例如目标人数、锁单人数、已完成人数、队伍有效期和通知配置。
    -->
    <resultMap id="dataMap" type="com.hjs.study.infrastructure.dao.po.GroupBuyOrder">
        <id column="id" property="id"/>
        <result column="team_id" property="teamId"/>
        <result column="activity_id" property="activityId"/>
        <result column="source" property="source"/>
        <result column="channel" property="channel"/>
        <result column="original_price" property="originalPrice"/>
        <result column="deduction_price" property="deductionPrice"/>
        <result column="pay_price" property="payPrice"/>
        <result column="target_count" property="targetCount"/>
        <result column="complete_count" property="completeCount"/>
        <result column="lock_count" property="lockCount"/>
        <result column="status" property="status"/>
        <result column="valid_start_time" property="validStartTime"/>
        <result column="valid_end_time" property="validEndTime"/>
        <result column="notify_type" property="notifyType"/>
        <result column="notify_url" property="notifyUrl"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!--
        新增一条拼团队伍主单。
        开新团时会调用这里创建队伍记录，初始状态固定写为 0（拼单中）。
    -->
    <insert id="insert" parameterType="com.hjs.study.infrastructure.dao.po.GroupBuyOrder">
        insert into group_buy_order(
        team_id, activity_id, source, channel, original_price,
        deduction_price, pay_price, target_count, complete_count,
        lock_count, status, valid_start_time, valid_end_time, notify_type, notify_url, create_time, update_time
        ) values(
        #{teamId}, #{activityId}, #{source}, #{channel}, #{originalPrice},
        #{deductionPrice}, #{payPrice}, #{targetCount}, #{completeCount},
        #{lockCount}, 0, #{validStartTime}, #{validEndTime}, #{notifyType}, #{notifyUrl}, now(), now()
        )
    </insert>

    <!--
        队伍锁单人数 +1。
        通过 lock_count < target_count 保护，避免超出队伍目标人数。
    -->
    <update id="updateAddLockCount" parameterType="java.lang.String">
        <![CDATA[
            update group_buy_order
            set lock_count = lock_count + 1, update_time= now()
            where team_id = #{teamId} and lock_count < target_count
        ]]>
    </update>

    <!--
        队伍已支付人数 +1。
        通过 complete_count < target_count 保护，避免重复累计超过目标值。
    -->
    <update id="updateAddCompleteCount" parameterType="java.lang.String">
        <![CDATA[
            update group_buy_order
            set complete_count = complete_count + 1, update_time= now()
            where team_id = #{teamId} and complete_count < target_count
        ]]>
    </update>

    <!-- 队伍锁单人数 -1，通常用于锁单失败、回滚或库存释放场景。 -->
    <update id="updateSubtractionLockCount" parameterType="java.lang.String">
        update group_buy_order
        set lock_count = lock_count - 1, update_time= now()
        where team_id = #{teamId} and lock_count > 0
    </update>

    <!--
        将队伍状态更新为已完成。
        只有当前仍处于拼单中的队伍才允许被更新，避免重复成团。
    -->
    <update id="updateOrderStatus2COMPLETE" parameterType="java.lang.String">
        update group_buy_order
        set status = 1, update_time= now()
        where team_id = #{teamId} and status = 0
    </update>

    <!-- 查询单个队伍的核心进度数据，用于判断是否满员、是否达成目标。 -->
    <select id="queryGroupBuyProgress" parameterType="java.lang.String" resultMap="dataMap">
        select target_count, complete_count, lock_count from group_buy_order
        where team_id = #{teamId}
    </select>

    <!-- 查询指定队伍的完整主单信息，常用于交易域构建 GroupBuyTeamEntity。 -->
    <select id="queryGroupBuyTeamByTeamId" parameterType="java.lang.String" resultMap="dataMap">
        select team_id, activity_id, target_count, complete_count, lock_count, status, valid_start_time, valid_end_time,
        notify_type, notify_url
        from group_buy_order where team_id = #{teamId}
    </select>

    <!-- 批量查询多个队伍主单，用于页面展示或超时任务补齐队伍信息。 -->
    <select id="queryGroupBuyTeamByTeamIds" parameterType="java.util.Set" resultMap="dataMap">
        select team_id, activity_id, target_count, complete_count, lock_count, status, valid_start_time, valid_end_time,
        notify_type, notify_url
        from group_buy_order where team_id in
        <foreach item="teamId" collection="teamIds" open="(" separator="," close=")">
            #{teamId}
        </foreach>
    </select>

    <!--
        批量查询“仍在进行中”的队伍。
        过滤条件包括：
        1. status = 0，仍在拼单中；
        2. target_count > lock_count，队伍未满员；
        3. valid_end_time > now()，队伍尚未超时。
    -->
    <select id="queryGroupBuyProgressByTeamIds" parameterType="java.util.Set" resultMap="dataMap">
        select team_id, activity_id, target_count, complete_count, lock_count, status, valid_start_time, valid_end_time,
        notify_type, notify_url
        from group_buy_order
        where status = 0 and target_count > lock_count and valid_end_time > now() and team_id in
        <foreach item="teamId" collection="teamIds" open="(" separator="," close=")">
            #{teamId}
        </foreach>
    </select>

    <!-- 统计指定队伍集合的总队伍数。 -->
    <select id="queryAllTeamCount" parameterType="java.util.Set" resultType="java.lang.Integer">
        select count(id) from group_buy_order
        where team_id in
        <foreach item="teamId" collection="teamIds" open="(" separator="," close=")">
            #{teamId}
        </foreach>
    </select>

    <!-- 统计指定队伍集合中已成团的队伍数量。 -->
    <select id="queryAllTeamCompleteCount" parameterType="java.util.Set" resultType="java.lang.Integer">
        select count(id) from group_buy_order
        where status = 1 and team_id in
        <foreach item="teamId" collection="teamIds" open="(" separator="," close=")">
            #{teamId}
        </foreach>
    </select>

    <!--
        汇总指定队伍集合的锁单人数。
        这里统计的是 lock_count 总和，而不是唯一用户数。
    -->
    <select id="queryAllUserCount" parameterType="java.util.Set" resultType="java.lang.Integer">
        select sum(lock_count) from group_buy_order
        where team_id in
        <foreach item="teamId" collection="teamIds" open="(" separator="," close=")">
            #{teamId}
        </foreach>
    </select>

    <!--
        未支付退单时回滚队伍锁单数。
        该 SQL 只允许处理仍处于拼单中的队伍。
    -->
    <update id="unpaid2Refund" parameterType="com.hjs.study.infrastructure.dao.po.GroupBuyOrder">
        update group_buy_order
        set lock_count = lock_count + #{lockCount}, update_time= now()
        where
        team_id = #{teamId} and
        status = 0
    </update>

    <!--
        已支付但未成团时的退单回滚。
        会同时调整锁单人数和已完成人数。
    -->
    <update id="paid2Refund" parameterType="com.hjs.study.infrastructure.dao.po.GroupBuyOrder">
        update group_buy_order
        set lock_count = lock_count + #{lockCount},
        complete_count = complete_count + #{completeCount},
        update_time= now()
        where
        team_id = #{teamId} and
        status = 0
    </update>

    <!--
        已成团后发生退单，但队伍仍有其他有效成员时，
        将队伍更新为“完成含退单”状态。
    -->
    <update id="paidTeam2Refund" parameterType="com.hjs.study.infrastructure.dao.po.GroupBuyOrder">
        update group_buy_order
        set lock_count = lock_count + #{lockCount},
        complete_count = complete_count + #{completeCount},
        status = 3,
        update_time = now()
        where
        team_id = #{teamId} and (status = 1 or status = 3) and complete_count > 1
    </update>

    <!--
        已成团后发生退单，且退款后队伍不再满足成团条件时，
        将队伍更新为失败状态。
    -->
    <update id="paidTeam2RefundFail" parameterType="com.hjs.study.infrastructure.dao.po.GroupBuyOrder">
        update group_buy_order
        set lock_count = lock_count + #{lockCount},
        complete_count = complete_count + #{completeCount},
        status = 2,
        update_time= now()
        where
        team_id = #{teamId} and (status = 1 or status = 3) and complete_count = 1
    </update>

</mapper>
