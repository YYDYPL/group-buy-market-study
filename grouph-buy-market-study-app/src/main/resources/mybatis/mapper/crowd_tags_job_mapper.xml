<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.bugstack.infrastructure.dao.ICrowdTagsJobDao">

    <!--
        crowd_tags_job 表结果映射。
        这张表保存“某个标签批次该如何计算”的任务配置，
        包括统计类型、规则阈值和统计时间窗口。
    -->
    <resultMap id="dataMap" type="cn.bugstack.infrastructure.dao.po.CrowdTagsJob">
        <id column="id" property="id"/>
        <result column="tag_id" property="tagId"/>
        <result column="batch_id" property="batchId"/>
        <result column="tag_type" property="tagType"/>
        <result column="tag_rule" property="tagRule"/>
        <result column="stat_start_time" property="statStartTime"/>
        <result column="stat_end_time" property="statEndTime"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!--
        查询指定标签、指定批次的人群任务配置。
        当前 SQL 只取标签计算真正需要的几个核心字段，
        用于构建领域层的 CrowdTagsJobEntity。
    -->
    <select id="queryCrowdTagsJob" parameterType="cn.bugstack.infrastructure.dao.po.CrowdTagsJob" resultMap="dataMap">
        select tag_type, tag_rule, stat_start_time, stat_end_time
        from crowd_tags_job
        where tag_id = #{tagId} and batch_id = #{batchId}
    </select>

</mapper>
